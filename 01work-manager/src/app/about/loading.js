//this component will trigger at the place of children component (page.js in this case) during the loading time

export default function Loading(){
  return(
    <div>
      <h1>Loading...</h1>
    </div>
  )
}